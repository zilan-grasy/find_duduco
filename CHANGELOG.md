# 寻找嘟嘟可 — 更新日志

## v1.3.0 (2026-06-11)

### 重做：自动识别
- **完全移除**旧的 `board_detect.py` 全屏自动检测方案（方向梯度投影，几乎不可用）
- **全新校准匹配模式**：为 6×6 ~ 15×15 每种尺寸预先框选棋盘位置，自动识别时逐一尝试
- **自动回退**：最大尺寸无解时自动尝试下一尺寸，直到找到解或全部失败
- 界面显示「回退 2/N」等实时进度，`recognize_and_solve()` 返回 `bool` 供调用方决策

### 新增：可配置点击速度
-「点击嘟嘟可」从硬编码延迟升级为 **5 个可调参数**，毫秒级精细控制：
  - `init_delay`（截图→首点）、`move_press`（移动→按下）、`press_release`（按下→抬起）
  - `click_gap`（双击间隔）、`duduco_gap`（位点间隔）
- 每个参数配备滑动条（`ttk.Scale`）+ 手动输入框，拖动时实时同步
- 「恢复默认」按钮一键重置所有间隔

### 新增：「首次点击点 3 下」开关
- 默认打钩，关掉后所有嘟嘟可位置均只点 2 下
- 「截图后立即点击」**拆分为两个独立开关**：「截图识别后」和「自动识别后」分别控制

### 重构：设置界面
- 工具栏新增 **「设置」按钮**，统一收纳校准、间隔、自动三项配置
- **不弹窗、不遮罩**：设置页面直接替换主内容区，点「← 返回」切回主界面
- **左侧竖直标签栏**（校准 / 间隔 / 自动），`tk.Label` + 浅紫背景高亮当前选中

### 重构：统一配置系统
- **`calibration.json` → `userdata/config.json`**：更名并移入 `userdata/` 目录
- `_load_settings` / `_save_settings` 统一读写所有配置字段
- 固定顺序：calibrations → click_timings → auto_click_capture → auto_click_auto → first_click_3 → puzzle
- 识别后自动保存 puzzle；校准 bbox、谜题 grid 采用紧凑单行格式

### UI 细节
- 标签栏背景 `lightgray`（与工具栏统一），解题结果框字体回归系统默认
- 间隔行 `ms` 紧贴数字框右侧，清除全部 / 恢复默认按钮与工具栏按钮同高
- 移除「解题结果将显示在这里」占位文本，三个设置页底部留空不贴底

### 文件结构与打包
- `userdata/` 统一存放程序生成文件和测试图片
- `.gitignore`：`userdata/config.json` 排除，测试图片正常提交
- `寻找嘟嘟可.spec`：添加 `datas`（userdata/）和 `hiddenimports`
- 打包后 exe 首次运行自动在 exe 同级创建 `userdata/` 目录

### 代码清理
- `import ctypes` 提至文件顶部（避免 try 块中的潜在 NameError）
- 移除死代码

---

## v1.2.0 (2026-05-11)

### 重构
- **消除代码重复**：`find_duduco.py` 通过 `import` 关联三个子模块，不再内联相同逻辑
- `GridImageRecognizer` 精简为状态持有者（~35行），识别/检测委托子模块执行
- 移除内联导入（`cv2`、`numpy`、`collections.defaultdict`、`typing` 等）
- `DuducoPuzzleSolver` 不再内联定义，从 `duduco_solve.py` import

### 约束规则统一
- **规则2 升级为双向鸽巢原理**：N 色跨 N 行/列时，同时执行占位排除（该范围其他色排除）和定域排除（该 N 色在其他范围也排除）
- **规则4（全行/列同色）并入规则2**：规则4 是双向鸽巢 n=1 的特例
- `_detection_loop` 从 4 条规则简化为 3 条

### 新增
- **README.md**：完整文档（功能介绍、快速开始、测试、独立模块用法、求解算法、常见问题、免责声明、MIT 许可证）
- **LICENSE**：MIT 开源许可
- **`test1_output.txt`**：文本格式示例（test1.png 的识别结果），供 `duduco_solve.py` CLI 测试
- **子模块 CLI 入口**：`grid_recog.py`（图像→文本）、`duduco_solve.py`（文本→解法）可独立命令行运行
- **虚拟环境打包支持**：`.vscode/settings.json` 配置虚拟环境解释器路径
- 全部文件添加中文注释（类/方法 docstring + 行内标注）

### 优化
- 清理未使用变量（`self.grid`、`compactness`、`best_labels`、`import sys` 等）
- 清理孤立注释残留
- 恢复方法的 BETA 标识

---

## v1.1.0 (2026-05-10)

### 新增
- **自动识别(beta)** 按钮：全屏截图 → 方向梯度投影 → 自动裁剪棋盘
- **点击嘟嘟可** 按钮：解题后自动移动鼠标到屏幕位置并双击
- **整行/列同色排除** 规则：某行/列全为同一颜色时，该色在其他位置的格子全部排除
- **15×15 网格上限** 守卫
- **独立模块文件**：`board_detect.py`、`grid_recog.py`、`duduco_solve.py`（可独立调用）

### 改动
- 合并两个旧网格线检测为一个 `_find_grid_lines`
- `recognize_from_image` 输出网格线坐标供 `get_cell_center` 使用
- `ScreenshotsSelector` 记录框选 bbox
- 双击间隔调优（避免游戏只识别一次点击）
- 移除调试 print 语句

### 删除
- `grid_recognizer_gui.py`、`solve_puzzle.py`

---

## v1.0.0 (2026-05-08)

基线版本，从零构建。

### 功能

**GUI 主界面**
- Tkinter 窗口，左上显示原图，左下显示识别出的颜色矩阵，右侧显示解题结果
- **打开图像**：选择本地 PNG/JPG 文件直接识别求解
- **截图识别**：全屏半透明遮罩 + 鼠标拖动框选棋盘区域 → 截取 → 识别 → 求解
- **置顶**：窗口始终保持在最前，方便对照游戏操作

**图像网格识别** — `GridImageRecognizer`
- Canny 边缘检测 → 行/列投影提取峰值 → Hough 直线补充 → 聚类合并去重 → 切出每个格子的子图
- 每个格子取中心 80% 区域（避开空隙边缘），HSV 均值取色
- 白/灰判定：饱和度 < 10 或 明度 > 245 且饱和度 < 25 视为空白格（0）
- K-means 聚类将相似颜色归为一类，Union-Find 合并距离 < 0.08 的近色簇

**嘟嘟可求解算法** — `DuducoPuzzleSolver`
- BFS 连通域：按颜色找出每个颜色的所有连通块
- 约束传播（不动点迭代）：
  - 规则① 最后一格：某行/列只剩 1 个合法位置 → 必放嘟嘟可，排除同行/列/相邻 8 格
  - 规则② 鸽巢原理：N 种颜色的候选位置仅跨 N 行/N 列 → 该 N 行/N 列内其他颜色排除
  - 规则③ 包围排除：某颜色的剩余合法格全落在同一格的 8 邻域内 → 该位置排除
- 双色 mini 回溯：约束传播卡住时取候选最少的 2 色穷举，跑完约束后交叉验证公共发现
- 主回溯：对剩余颜色递归穷举，保存/恢复状态

### 附属文件
- `grid_recognizer_gui.py`：独立的图像识别 GUI（已删除于 v1.1.0）
- `solve_puzzle.py`：命令行求解器（已删除于 v1.1.0）
